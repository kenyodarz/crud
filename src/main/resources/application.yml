spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bdejemplo?serverTimezone=UTC
    username: root
    password: programador
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      format_sql: true
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false